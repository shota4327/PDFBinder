# PDF Binder プロジェクト開発・コーディング原則

本ドキュメントは、PDF Binder の開発における絶対的な設計原則、コーディング規約、運用制約、および自律改善ルールを定義したものです。本プロジェクト内のすべてのコード変更およびエージェントの動作は、以下のルールに厳格に従う必要があります。

---

## 1. 言語およびドキュメンテーション規約
- **対話・説明言語**: ユーザーとの対話、実装計画、タスク、Walkthrough、コミットメッセージ、Git操作の説明文はすべて**日本語**で記述すること。
- **コード内コメント**: クラス・メソッド・プロパティのXMLドキュメントコメント（`/// <summary>` 等）、インラインコメント、テストケースの説明文を含め、コード内のコメントはすべて**日本語**で記述すること。
- **命名規則**: クラス名、メソッド名、プロパティ名、変数名は C# / .NET の標準コーディング規約（PascalCase, camelCase、プライベートフィールドの `_camelCase` 等）に従い、明確で分かりやすい**英語**で命名すること（ローマ字命名は禁止）。
- **プルリクエスト・Issue作成規則**:
  - `gh pr create` / `gh pr edit` / `gh issue create` / `gh issue edit` 時は、シェルエスケープによる文字化け（`\b`, `\f`, `\v`, `\e`, `\a` などの制御コード化やバッククォートの消失）を完全に防止するため、必ずエディタ/ファイル書き出しツール（または単一引用符ヒアストリング `@'... '@`）で UTF-8 マークダウンファイルを作成し、`--body-file` オプション経由で本文を渡すこと。

---

## 2. アーキテクチャおよびコーディング原則 (C# / WPF / .NET 10.0)
- **MVVM パターンの徹底**:
  - View（XAML / コードビハインド）と ViewModel / Model（業務ロジック・PDF処理）の責務を明確に分離すること。
  - CommunityToolkit.Mvvm 等の標準的かつ軽量なライブラリを活用し、疎結合でテスト容易な設計を保つこと。
- **単一責任の原則（SRP）**:
  - 各クラス・メソッド・サービスは1つの明確な責務に集中させること。PDF操作、レンダリング、インク管理、UI表示を同一クラスに混在させない。
- **メソッドの行数制限（目安30〜50行）**:
  - 各メソッドは**30〜50行以内を目安**に適度に分割すること。
  - 巨大なメソッド（100行以上）を放置せず、座標変換、ストローク処理、ファイル入出力などの処理単位でプライベートメソッドやヘルパークラスに分離すること。
- **最小差分原則（Minimal Diff）とファイル全体上書きの禁止**:
  - 既存ファイルの修正時は、変更が必要なメソッド・クラス・行ブロックのみを対象とし、不必要なファイル全体の再書き出し（丸ごと上書き）や広範囲な無関係コードの一括置換を行わないこと。
  - 差分（Git diff）を最小限かつ局所化し、コードレビューの可読性と変更の追跡性を最高水準に保つこと。
  - 修正時は必ず対象箇所の前後の行や文脈を確認し、変更対象外のコードやコメントに不要な変更を混入させないこと。
- **C# の厳格な型安全性と Null 許容参照型対策**:
  - `#nullable enable` を標準とし、null の可能性をコンパイル時に検知・排除すること。
  - null合体演算子（`??`）、null条件演算子（`?.`）、パターンマッチング（`is` / `switch`）を活用し、`NullReferenceException` を徹底して防止すること。
- **リソース管理とメモリリーク防止**:
  - PDFドキュメント、ビットマップ（`BitmapSource`）、画像ストリームなどのアンマネージリソース・大容量オブジェクトは、`using` ステートメントまたは `IDisposable` 実装により確実に即時解放すること。
  - WPF イベントハンドラーの購読解除を徹底し、循環参照やメモリリークを防止すること。
- **ポータブル・オフライン前提設計**:
  - 外部ネットワーク接続を前提とせず、完全オフラインで軽快に動作する構成とすること。
  - 配布形態として、依存ランタイムを同梱した単一実行ファイル（Self-Contained Single-File EXE）またはZIP解凍のみで動作するポータブル設計を基本とすること。

---

## 3. 重要な運用・プロセス制約（厳守事項）

### 3.1 Git & ブランチ・プルリクエスト（PR）運用ルール
- **Issue 駆動開発 & ブランチ運用フロー**:
  - 新機能追加や不具合修正はもちろん、**ドキュメント（`GEMINI.md`、`README.md`、`docs/*` 等）の更新のみであっても例外なく必ず GitHub Issue をもとにブランチを作成** して進めること（`master` ブランチへの直接コミットは厳禁）。
  - ブランチ命名規則: `issue-<番号>-<概要>`、`feature/issue-<番号>-<概要>`、`fix/issue-<番号>-<概要>` 等。
- **コミット時の同時プッシュ（必須）**:
  - 作業ブランチおよび `master`（または `main`）ブランチを問わず、ローカルでコミット（`git commit`）を作成した際は、必ず同時にリモートへプッシュ（`git push` / `git push -u origin <branch>`）を実行し、リモートとローカルを常に最新状態で同期させること。
- **プルリクエスト（PR）の作成・マージに関する絶対ルール**:
  - **勝手にプルリクエストを作成またはマージすることは厳禁**。
  - 実装計画の承認、コード実装、単体テスト・ビルド検証がすべて完了した後は、まず作業ブランチ上の成果と検証結果をユーザーに報告し、レビューと指示を待つこと。
  - `gh pr create` によるプルリクエスト作成および `gh pr merge` によるマージは、必ず**ユーザーからの明示的な指示（例:「プルリクをお願いします」「PRを作成してください」等）を受けてからのみ実行すること**。
  - **マージ方式（スカッシュマージの禁止・通常マージの厳守）**:
    - プルリクエストをマージする際は、**スカッシュマージ（`--squash`）を行ってはならない（禁止）**。
    - 各機能ブランチで作成した個別コミット履歴を完全に保持するため、必ず**通常マージ（`gh pr merge <PR番号> --merge --delete-branch`）を使用すること**。
  - **PR作成時の自律的バージョンインクリメント＆マージ後のGitタグ作成（必須・方式①）**:
    - **`master` ブランチへの直接コミットは例外なく厳禁**。バージョン更新および CHANGELOG 更新も必ず作業ブランチ内で実施し、PRレビューを経てマージすること。
    - 各Issue/PRの作業ブランチにおいて、該当PRの変更内容に応じて `Directory.Build.props` のバージョンを自律的にインクリメントすること:
      - 新機能追加・機能拡張を含む場合: マイナーバージョン（`0.X.0`）を1繰り上げ、パッチを0リセット（例: `0.1.0` → `0.2.0`）
      - 不具合修正・細かな改善・ドキュメント更新のみの場合: パッチバージョン（`0.X.Y`）を1繰り上げ（例: `0.1.0` → `0.1.1`）
      - メジャーバージョン（`1.0.0` 等）への繰り上げは、ユーザーからの明示的な指示があった時のみ実施すること。
    - 作業ブランチにおいて、`CHANGELOG.md` に新バージョンセクション（`## [X.Y.Z] - YYYY-MM-DD`）を確定・記述し、最上部に新たな空の `## [Unreleased]` セクションを配置すること（PRの差分でバージョン番号と変更履歴の事前レビューを可能とする）。
      - **CHANGELOG記述方針（エンドユーザー向け記述の厳守）**: `CHANGELOG.md` は一般利用者（エンドユーザー）向けのリリースノートであることを前提とし、機能や操作性、改善点のみをシンプルに記述すること。内部のファイル名（例: `App.xaml`）、クラス名/コントロール名（例: `AboutOverlayControl`）、スタイル名（例: `ModalCardBorderStyle`）、コード上の寸法（例: `12px`）などの開発者向け実装詳細は記述してはならない。
    - PRマージ後は、`git checkout master`、`git pull origin master`、`git fetch --prune` を順番に実行してローカルを最新化する。
    - **`master` 上ではコミットを一切行わず**、最新化されたマージコミットに対して直ちに対応するGitタグ（`vX.Y.Z`）を作成してリモートへプッシュ（`git tag vX.Y.Z`、`git push origin vX.Y.Z`）すること。
  - **マージ完了時の配布バイナリ自動発行（必須）**:
    - 上記のGitタグ付け完了後、必ず `powershell -ExecutionPolicy Bypass -File .\build.ps1`（配布バイナリ発行スクリプト）を実行し、`dist/`（フレームワーク依存版: DLL分離形式）および `dist/self-contained/PDFBinder.exe`（自己完結版: 単一EXE形式）に最新の配布ファイルを書き出し・更新すること。
    - 生成された各バイナリ・フォルダの出力パス、バージョン、およびサイズをユーザーへの作業完了報告に明記すること。

### 3.2 実装計画（Plan）＆検証報告（Walkthrough）のライフサイクル
- **Plan & Walkthrough の永続記録と同時コミット（必須）**:
  - 各タスク・機能開発で作成する「Implementation Plan（実装計画）」および「Walkthrough（検証報告）」は、単発で破棄せず `docs/plans/` ディレクトリ内に Markdown ファイルとして保存・蓄積すること。
  - **ファイル命名規則**:
    - **初回実装時**:
      - 実装計画: `docs/plans/issue-<Issue番号>-<タスク名>-plan.md`
      - 検証報告: `docs/plans/issue-<Issue番号>-<タスク名>-walkthrough.md`
    - **同一Issue内での追加改修・追加作業時（複数Plan発生時）**:
      - 既存のPlan/Walkthroughファイルを絶対に上書き・破棄せず、追加作業ごとに独立した別ファイルとして保存すること。
      - 追加タスク名に続けて末尾に連番（2回目作業なら `-2`, 3回目作業なら `-3`）を付与する。
      - 実装計画: `docs/plans/issue-<Issue番号>-<追加タスク名>-<連番>-plan.md`
      - 検証報告: `docs/plans/issue-<Issue番号>-<追加タスク名>-<連番>-walkthrough.md`
- **Plan策定フェーズ（作業開始前）**:
  - プラン策定時はアーティファクト作成と同時に `docs/plans/` 配下に Plan ファイルを作成すること。
  - ユーザーからのフィードバックや指示により計画内容を変更・修正する場合は、アーティファクトおよび `docs/plans/` 配下の Plan ファイルを都度最新の内容に同期・修正すること。
  - **Plan単体での先行コミットは不要（禁止）**: 実装計画ファイル単体でのコミット・プッシュは行わず、実装コード・テスト・Walkthroughとまとめて同時にコミットすること。
- **実装・検証フェーズ（作業開始後）**:
  - **Planファイルの上書き禁止（完全凍結）**:
    - コード実装を開始した後は、**既存のPlanファイル（`*-plan.md`）は一切変更・上書きを行わないこと（完全凍結）**。過去の設計意図や検討経緯の履歴をそのまま永久保存すること。
  - **Walkthroughファイルの更新・上書きルール**:
    - **計画（Plan）を新規作成しない程度の軽微な修正時（バグ修正、微細なレイアウト調整、テストコード調整等）**:
      - 直近の最新 Walkthrough ファイル（追加改修作業中であればその連番の `*-<連番>-walkthrough.md`、初回後であれば `*-walkthrough.md`）およびアーティファクトを**直接加筆・上書き修正**し、最新の変更内容と検証結果を正確に反映させること。
    - **新たな要件変更・追加機能・大きな設計見直しが発生した場合（Plan作成を伴う作業）**:
      - 過去のPlanファイルを絶対に上書きせず、上記「同一Issue内での追加改修・追加作業時」の規則に従って連番付きの新規Plan（`*-<連番>-plan.md`）および新規Walkthrough（`*-<連番>-walkthrough.md`）を新たに起票・記録し、追加実装サイクルを回すこと。

### 3.3 ツール使用 & コマンド実行制約
- **単一コマンド実行原則（コマンド連結記号 `&` / `&&` / `;` / `|` の禁止）**:
  - シェルコマンド実行ツール（`run_command`）を使用する際は、`&`, `&&`, `;`, `|` などの連結記号を用いて複数のコマンドを1行にまとめて実行することを**全面的に禁止**する。
  - PowerShell / 各種シェル環境において、コマンド連結は各コマンドの成否（Exit Code）や標準出力／標準エラー出力の追跡を困難にし、途中で失敗した場合に予期しない挙動や誤った状態遷移を引き起こす原因となる。
  - すべてのコマンド（例: `git checkout`、`git pull`、`dotnet test`、`dotnet build` 等）は、必ず**1つのツール呼び出しにつき単一のコマンドとして独立して順番に実行すること**。
- **ファイル作成・編集時の専用ツール使用原則（シェルコマンド経由の禁止）**:
  - ファイルの新規作成、編集、削除などのファイル操作を行う際は、シェルコマンド（PowerShell, bash, `echo`, `Out-File`, `Set-Content`, `sed`, `cat` 等）を介さず、必ず直接ファイル操作ツール（`write_to_file`, `replace_file_content` 等）を使用すること。
  - シェルコマンド経由でのファイル書き込みは、不要なコマンド実行承認プロンプトの発生やエスケープ起因の文字化け・フォーマット破壊の原因となるため厳禁とする。
- **バックグラウンドコマンド実行時のポーリング・タイマー設定の厳禁（Reactive Wakeupの厳守）**:
  - `dotnet test` やビルド等の長時間コマンドをバックグラウンド実行（`run_command` がタスクIDを返却）した際は、**`schedule` ツールによるタイマー呼び出しや `manage_task (status)` による定期ポーリングを一切行ってはならない（厳禁）**。
  - コマンド完了時にシステムから自動的に通知メッセージ（Reactive Wakeup）が直接コンテキストに届くため、コマンド起動後は**追加のツール呼び出しを一切行わずにターンを終了して待機すること**。これにより無駄なプロンプト実行やトークン枠の浪費を完全に防止する。
- **並列プロセスの多重起動防止**:
  - ビルドやテスト実行時は既存プロセスの状態を確認し、バックグラウンドでの無駄な重複起動を防ぐこと。

---

## 4. ドメイン機能・仕様の基本原則 (PDF Binder)
本プロジェクトにおける主要機能の基本方針は以下の通りとする。具体的なクラス構成、データ構造、UI設計の詳細については、Single Source of Truth（正本）である**基本設計書（`docs/basic_design.md`）**を厳格に遵守すること。

1. **グリッド俯瞰ビュー（バインダー概要）**:
   - 開いたPDFの全ページを高解像度サムネイルとしてタイル状グリッドに一覧表示する。
   - ドラッグ＆ドロップによる直感的なページ並び替え、複数選択、回転（時計回り・反時計回り・180度）、削除をサポートする。
   - グリッド上の操作はすべて Undo/Redo（Ctrl+Z / Ctrl+Y）に対応すること。
2. **詳細手書きエディタ（ページ編集）**:
   - ページをダブルクリックすることで、全画面または専用エディタビューへ切り替えて詳細表示する。
   - WPF `InkCanvas` を基盤とし、ペン、蛍光ペン（半透明描画）、消しゴム（ストローク消し／部分消しの切替）、直線ツールを完備する。
   - 自由な拡大・縮小（ズーム）およびパン（手のひらツール）、手書き操作の Undo/Redo に対応する。
   - 保存時はベクター情報および透過高解像度描画のハイブリッドにより、劣化のない高画質アノテーションとしてPDFへ反映すること。
3. **ファイル操作・結合・分割・空白追加**:
   - **結合**: ツールバーからの追加、または外部PDFファイルのグリッドへの直接ドラッグ＆ドロップにより、任意の位置にページを差し込み可能とする。
   - **分割**: 選択ページの別ファイル書き出し（抽出）、および1ページずつの個別PDF一括分割をサポートする。
   - **空白追加**: 選択位置または末尾に、隣接ページの用紙サイズ・向きを継承した白紙ページを追加可能とする。
   - **ファイルロック回避と非破壊編集**: 開いているPDFファイルをロックせず、安全な「上書き保存」および「別名で保存」を実現する。
4. **ライセンス厳守（OSS公開前提）**:
   - 使用するライブラリは、商用・非商用問わず自由に再配布可能な**MITライセンス、Apache-2.0、BSDライセンス等**に限定する。
   - AGPLなどの強力なコピーレフトや商用利用に制限のあるライブラリは一切採用しない。
   - PDF操作エンジン: `PdfSharp` (MIT)
   - PDFレンダリングエンジン: `PDFium` (Apache-2.0 / BSD)
5. **アプリアイコン**:
   - プロジェクトルートの `icon.png` を正本アイコンとし、ウィンドウアイコン、タスクバーアイコン、および実行可能ファイルのリソースアイコンとして設定すること。

---

## 5. ドキュメントおよびルールの自律的・継続的アップデート
- **プロジェクトドキュメントの同期更新**:
  - 新機能の追加、仕様変更、画面レイアウトやUIの変更を行った際は、コード変更だけでなく以下の関連ドキュメントも**自律的に最新状態へ更新・同期すること**:
    - `docs/basic_design.md`（基本設計書）: 仕様変更、新機能、画面構成等の反映
    - `README.md`: アプリ概要、機能一覧、動作要件、ビルド手順等の反映
    - `docs/PROJECT.md`: 機能インベントリ（Feature Inventory）のステータス更新
    - `CHANGELOG.md`: 各Issue/PRの作業ブランチにおいて、加えた変更内容に応じて新バージョン（`## [X.Y.Z] - YYYY-MM-DD`）を確定・記述し、最上部に空の `## [Unreleased]` を配置すること。記述内容はエンドユーザー向けとし、内部のファイル名・クラス名・スタイル名・px寸法などの開発者向け実装詳細を省き、機能・操作性・改善点のみをシンプルに記述すること。
- **過去の設計・実装記録の参照と一貫性維持（Single Source of Truth）**:
  - 新機能開発や類似ロジックの修正・リファクタリング時は、`docs/plans/` 配下に蓄積された過去の Plan および Walkthrough を参照し、設計意図や過去の判断・仕様決定を尊重して一貫性を保持すること。
- **ルールファイルの自律的見直し・改善**:
  - 開発過程で得られた新たな設計方針、ユーザーからのフィードバックや修正指示、再発防止策などがあれば、**AIエージェント自身が本ルールファイル（`GEMINI.md`）を自律的に見直し・追記・改善すること**。
  - ルールファイルやドキュメントを更新した場合は、作業完了時の報告にて「更新・改善した内容」をユーザーに明示すること。
- **プロアクティブな品質改善・リファクタリング**:
  - 作業過程で30〜50行制限の超過、重複コード、日本語コメントの欠落などを検知した場合は、ユーザーからの指摘を待つことなく自律的にリファクタリングを実施して高品質を維持すること。

---

## 6. 品質基準・完了条件
- **単体テストの自動追加と検証（必須）**:
  - PDF操作サービス（回転、削除、並び替え、結合、分割、空白追加）や各ViewModel等のロジック変更時は、**自動的に対応する単体テスト（xUnit / MSTest）を作成・追加・実行すること**。
  - 正常系、境界値、エッジケースを網羅し、テストスイートで全テストが 100% PASS することを必須とする。
- **コード変更後およびタスク完了時の必須確認事項**:
  1. `dotnet test`: 全単体テストがすべて PASS すること
  2. `dotnet build`: ビルドが警告およびエラーなく成功すること
